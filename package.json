{
  "name": "handrail",
  "private": true,
  "version": "0.0.0",
  "description": "WebMCP human consent and authority layer for AI agents — accessible, deterministic tool-trust and human-confirmation gate for screen-reader and keyboard-only users.",
  "type": "module",
  "scripts": {
    "test": "node tests/security-suite.js && node tests/authority-tests.js",
    "clean": "rm -rf dist server.js src"
  }
}
